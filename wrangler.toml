name = "image-bg-remover"
compatibility_date = "2024-03-18"

# 环境变量（生产环境需要在 Cloudflare Dashboard 中设置）
[vars]
# REMOVE_BG_API_KEY = "your_api_key_here"

# 路由规则
[[routes]]
pattern = "image-bg-remover.pages.dev"
custom_domain = true
